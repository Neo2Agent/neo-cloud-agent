/** Shared with the mobile artifact list; the logic lives in contracts so RN never imports web. */
export {
  artifactKind,
  artifactKindLabel,
  prettyBytes,
  previewKind,
  type ArtifactKind,
  type ArtifactPreviewKind,
} from "@neo-cloud-agent/contracts/artifact";
