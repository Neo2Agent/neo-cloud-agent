import { defineExtension } from "./types.js";

export const neoArtifact = defineExtension({
  name: "neo-artifact",
  description: "Upload screenshots, recordings, and logs through artifact-service.",
});
